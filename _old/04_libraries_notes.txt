Google Trends:
- probeer javascript angular, javascript react, javascript polymer
- ook grafieken onderaan tonen

Resource Loaders:
- script/styles loader - volg overview link
	. bespreek HeadJS syntax
		- oplijsting + callback parameter
		- of via ready() callback
		- via object: je kunt keys gebruiken om te specifiëren waarop je wil wachten
	. RequireJS:
		- eerste parameter is array, voor de rest ongeveer hetzelfde
	. YepNope: eerder bedoeld als conditional, in conjunctie met Modernizr, maar complete listener kan ook
	. LabJS: werkt met promises (modernere syntax, zie volgend semester)
	. LazyLoad: ultraklein (<1KB)
- media resources loader:
	. PreloadJS (tonen)
	. demos PXLoader tonen

Template engines:
- moustacheJS demo sourcecode vergelijken met DOM

Widgets:
- jQueryUI:
	. toon spinner + code
	. toon accordeon
	. toon checkboxradio
	. toon draggable
	. animate effect
- scriptaculous
	. animation framework
	. draggable
- DoJo: uncool documentatie (auto generated)
- Spritely:
	. animatie met sprite() methode -> redelijk specifiek; vaak zit er dan weer net niet in wat je nodig hebt
	. @see online sprite maker
- ThreeJS:
	. click 'examples' -> animation cloth, geometry cube


API JSON
-> vergeet GET/POST niet correct in te stellen
-> bij INSERT: raw JSON data + type JSON kiezen


